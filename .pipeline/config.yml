general:
  buildTool: maven

stages:
  Build:
    mavenExecuteStaticCodeChecks: true

steps:
  mavenExecute:
    goals: "clean install"
    buildTarget: "target"

  sonarExecuteScan:
    additionalParameters: "-Dsonar.java.binaries=target/classes"
general:
  buildTool: maven

stages:
  Build:
    mavenExecuteStaticCodeChecks: true

  Compliance:
    sonarExecuteScan: true

steps:
  sonarExecuteScan:
    parameters:
      waitForQualityGate: true   # ✅ THIS IS THE REAL FIX

  mavenExecute:
    goals: "clean install"
general:
  buildTool: maven

stages:
  Build:
    mavenExecuteStaticCodeChecks: false

  Additional Unit Tests:
    run: true

steps:
  mavenExecute:
    goals: "clean verify -Dtest=!AdditionalTest"

  additionalUnitTests:
    command: "mvn test -Dtest=AdditionalTest"

  cloudFoundryDeploy:
    cfApiEndpoint: "https://api.cf.ap21.hana.ondemand.com"
    cfOrg: "d38f4a1dtrial"
    cfSpace: "dev"
    cfAppName: "spring-mvc-app"
    deployType: "standard"
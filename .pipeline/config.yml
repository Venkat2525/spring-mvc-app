general:
  buildTool: maven

stages:
  Build:
    mavenExecuteStaticCodeChecks: true

steps:
  mavenExecute:
    goals: "clean install"

  sonarExecuteScan:
    dockerOptions: "-u 0"
    additionalParameters: "-Dsonar.java.binaries=target/classes"
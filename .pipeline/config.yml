general:
  buildTool: maven

stages:
  Build:
    mavenExecuteStaticCodeChecks: false

  AdditionalUnitTests:
    run: true

steps:

  mavenExecute:
    goals: "clean install -DskipTests=true"   # skip tests in build

  mavenExecuteAdditionalUnitTests:
    goals: "test"   # run tests here

  cloudFoundryDeploy:
    cfApiEndpoint: "https://api.cf.ap21.hana.ondemand.com"
    cfOrg: "d38f4a1dtrial"
    cfSpace: "dev"
    cfAppName: "spring-mvc-app"